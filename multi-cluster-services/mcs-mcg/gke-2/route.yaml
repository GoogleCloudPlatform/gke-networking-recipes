kind: HTTPRoute
apiVersion: networking.x-k8s.io/v1alpha1
metadata:
  name: bar-frontend-route-ilb
  namespace: mcs-mcg
  labels:
    gateway: multi-cluster-gateway-ilb
spec:
  hostnames:
  - "bar.example.com"
  rules:
  - forwardTo:
    - serviceName: bar
      port: 8080
      weight: 100